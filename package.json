{
  "name": "guardianx-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"require('fs').writeFileSync('.z-ai-config', process.env.ZAI_CONFIG || '{}')\" && bun --hot index.ts",
    "start": "node -e \"require('fs').writeFileSync('.z-ai-config', process.env.ZAI_CONFIG || '{}')\" && bun index.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "socket.io": "^4.8.1",
    "z-ai-web-dev-sdk": "latest"
  }
}
